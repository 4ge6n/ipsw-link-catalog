/* English is the language the keys are written in, so this file is empty on
   purpose: a key with no entry falls back to the key itself. It exists so that
   English is offered as a localization at all. */
